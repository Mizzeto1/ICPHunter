import { CompanyConfig } from "@/lib/types";

const config: CompanyConfig = {
  slug: "innovaccer",
  name: "Innovaccer",
  loomUrl: "",
  tagline: "How I'd build pipeline for Innovaccer's healthcare data platform",
  healthcareVertical: "Healthcare data platform — data unification, analytics, and AI-driven insights for value-based care",
  products: [
    "Health Cloud data platform",
    "InData — healthcare data unification engine",
    "InCare — care management and coordination",
    "InConnect — patient engagement and communication",
    "InNote — clinical documentation AI",
  ],
  idealCustomerProfile:
    "Health systems, ACOs, and payers transitioning to value-based care models with fragmented data across multiple EHRs, needing unified patient views and population health analytics.",
};

export default config;
